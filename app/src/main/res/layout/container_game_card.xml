<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/container_game_card_horizontal_margin"
        android:layout_marginEnd="@dimen/container_game_card_horizontal_margin"
        app:cardCornerRadius="10dp"
        app:cardElevation="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/imageViewGameHeader"
            android:layout_width="380dp"
            android:layout_height="@dimen/container_game_card_height"
            android:contentDescription="@string/game_picture"
            android:scaleType="centerCrop"
            tools:srcCompat="@sample/games.json/games/background[0]" />
    </androidx.cardview.widget.CardView>

    <TextView
        android:id="@+id/textViewGameHeaderTitle"
        style="@style/Widget.GamePicker.GameHeaderTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:layout_marginTop="16dp"
        android:background="@color/black"
        android:paddingStart="8dp"
        android:paddingEnd="8dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="@sample/games.json/games/name[0]" />

</androidx.constraintlayout.widget.ConstraintLayout>